# ============================================
# SHIB Meme代币合约部署配置
# ============================================
# 说明：复制此文件为 .env 并填写实际值

# ============================================
# 代币基本信息
# ============================================
# 代币名称
TOKEN_NAME=Shiba Inu

# 代币符号
TOKEN_SYMBOL=SHIB

# 代币总供应量（单位：代币数量，不是wei）
TOKEN_TOTAL_SUPPLY=1000000000

# ============================================
# 代币税配置
# ============================================
# 税率（基点，10000 = 100%，例如：500 = 5%）
TAX_RATE=500

# 税收接收地址（税费将发送到此地址）
# 如果设置为零地址或未设置，将自动使用部署者地址
TAX_RECEIVER=0x0000000000000000000000000000000000000000

# ============================================
# 网络配置
# ============================================
# 私钥（用于部署合约，请妥善保管，不要提交到代码仓库）
PRIVATE_KEY=your_private_key_here

# Sepolia测试网RPC URL
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/your_infura_key

# 主网RPC URL
# MAINNET_RPC_URL=https://mainnet.infura.io/v3/your_infura_key

# Etherscan API Key（用于验证合约）
ETHERSCAN_API_KEY=your_etherscan_api_key

# 是否自动验证合约（true/false）
# 设置为 true 时，部署完成后会自动在 Etherscan 上验证合约源代码
AUTO_VERIFY=true

# ============================================
# 通用配置
# ============================================
# 零地址（Zero Address）
# 以太坊中的零地址，用于表示空地址或未初始化的地址
# 通常用于：地址验证、默认值检查、空值判断等场景
# 标准零地址：0x0000000000000000000000000000000000000000
# 注意：不建议修改此值，除非有特殊需求
ZERO_ADDRESS=0x0000000000000000000000000000000000000000


# ============================================
# Uniswap V2配置(工厂合约地址和V2Router02合约地址)
# ============================================
# Uniswap V2 Router地址
# 工厂合约地址和V2Router02合约地址支持网络查询
# https://docs.uniswap.org/contracts/v2/reference/smart-contracts/v2-deployments
# 这里设置的Sepolia网络
UNISWAP_V2_ROUTER=0xeE567Fe1712Faf6149d80dA1E6934E354124CfE3
# Uniswap V2 Factory地址（可选）
# 如果不设置，部署脚本会通过 Router 的 factory() 函数自动获取
# 如果设置，将使用此地址（用于手动指定或修正 Factory 地址）
UNISWAP_V2_FACTORY=0xF62c03E08ada871A0bEb309762E260a7a6a880E6
# Uniswap V2 WETH地址（可选）
# 如果不设置，部署脚本会通过 Router 的 WETH() 函数自动获取并更新到此文件
# WETH 是 Wrapped ETH（包装的以太币），不同网络有不同的 WETH 地址
# 不同网络地址查询: https://docs.uniswap.org/contracts/v3/reference/deployments/ethereum-deployments#wrapped-native-token-addresses
UNISWAP_V2_WETH=


# 是否自动创建交易对（true/false）
# 这里是要默认开启的
AUTO_CREATE_PAIR=true

# Uniswap V2 Pair地址（交易对地址）
# 部署脚本会自动创建并填充此值，无需手动填写
# 如果 Pair 已存在，可以手动填写
UNISWAP_V2_PAIR=

# ============================================
# 流动性初始化配置（可选）
# ============================================
# 已部署的合约地址（用于初始化流动性脚本）
# 部署脚本会自动填充此值，无需手动填写
CONTRACT_ADDRESS=

# 初始代币数量（用于添加流动性，单位：代币数量，不是wei）
# 建议值：1000000（100万代币）
INITIAL_TOKEN_AMOUNT=1000000

# 初始ETH数量（用于添加流动性，单位：ETH，不是wei）
# 建议值：1（1 ETH）
INITIAL_ETH_AMOUNT=1
