# ============================================
# MetaStake-Token 环境变量配置模板
# ============================================
# 复制此文件为 .env 并填入实际值
# cp .env.example .env

# ============================================
# 网络配置
# ============================================

# Sepolia测试网RPC URL
# 可以从以下服务商获取:
# - Alchemy: https://www.alchemy.com/
# - Infura: https://www.infura.io/
# - QuickNode: https://www.quicknode.com/
SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_API_KEY

# 部署账户私钥（不要包含0x前缀）
# 警告: 永远不要将私钥提交到代码仓库！
PRIVATE_KEY=your_private_key_here_without_0x_prefix

# Etherscan API Key（用于验证合约）
# 获取地址: https://etherscan.io/apis
ETHERSCAN_API_KEY=your_etherscan_api_key

# ============================================
# 合约地址配置（部署后自动保存到deployments目录）
# ============================================

# MetaNode代币地址（部署后自动填充）
# 注意: 这是奖励代币地址，用户质押后会获得MetaNode代币作为奖励
# 如果手动设置，将覆盖deployments目录中的值
META_NODE_TOKEN_ADDRESS=

# MetaStake合约地址（部署后自动填充）
# 如果手动设置，将覆盖deployments目录中的值
META_STAKE_ADDRESS=

# 实现合约地址（部署后自动填充）
# 如果手动设置，将覆盖deployments目录中的值
IMPLEMENTATION_ADDRESS=

# ============================================
# 部署配置（用于deploy.js脚本）
# ============================================

# 每个区块的MetaNode奖励数量（默认: 1 MNODE）
# META_NODE_PER_BLOCK=1

# 初始奖励代币总量（默认: 1000000 MNODE）
# REWARD_AMOUNT=1000000

# ============================================
# 池配置（用于deploy.js和addPool.js脚本）
# ============================================
# 注意: 这些参数会被部署脚本和添加池脚本共享使用
# - deploy.js: 用于创建第一个池
# - addPool.js: 用于添加新池

# 最小质押金额（默认: 0.1 ETH）
# MIN_DEPOSIT=0.1

# 解质押锁定区块数（默认: 100）
# UNSTAKE_LOCKED_BLOCKS=100

# 池权重（默认: 1）
# POOL_WEIGHT=1

# ============================================
# 添加池配置（用于addPool.js脚本）
# ============================================
# 注意: 添加池时，会使用上面的池配置参数（MIN_DEPOSIT、UNSTAKE_LOCKED_BLOCKS、POOL_WEIGHT）
# 如果需要在添加池时使用不同的值，可以在运行addPool.js前临时设置这些环境变量

# 质押代币地址（用于创建新的质押池）
# 注意: 这是用户需要质押的代币地址，不是MetaNode代币地址！
# - address(0) 或 0x0000000000000000000000000000000000000000 表示Native Currency (ETH)
# - 其他地址表示ERC20代币地址（如USDT、USDC、WETH等）
# 示例:
#   ETH池: ST_TOKEN_ADDRESS=0x0000000000000000000000000000000000000000
#   USDT池: ST_TOKEN_ADDRESS=0xdAC17F958D2ee523a2206206994597C13D831ec7 (主网)
#   WETH池: ST_TOKEN_ADDRESS=0xC02aaA39b223FE8D0A0e5C4F27eAD9083C756Cc2 (主网)
# ST_TOKEN_ADDRESS=0x0000000000000000000000000000000000000000

# ============================================
# Uniswap配置
# ============================================

# Uniswap V2 Router地址（可选，部署后可以稍后设置）
# 主网: 0x7a250d5630B4cF539739dF2C5dAcb4c659F2488D
# Sepolia: 0xeE567Fe1712Faf6149d80dA1E6934E354124CfE3
# 其他网络请查看Uniswap官方文档
# 如果在此处设置，部署脚本会自动配置Router
# UNISWAP_ROUTER=0xeE567Fe1712Faf6149d80dA1E6934E354124CfE3

# 输出代币地址（用于testUniswap.js脚本测试交换成ERC20代币）
# 可选，如果不设置，testUniswap.js只会测试交换成ETH
# 示例: OUTPUT_TOKEN_ADDRESS=0x1234567890123456789012345678901234567890
# OUTPUT_TOKEN_ADDRESS=

# ============================================
# Gas报告配置
# ============================================

# 是否启用Gas报告（true/false）
REPORT_GAS=false

# ============================================
# 交互脚本配置（用于interact.js）
# ============================================

# 查询用户信息的地址（可选）
# 如果不设置，默认使用部署账户地址(默认调用PRIVATE_KEY查询出部署账户地址)
# 设置后，脚本会查询指定用户在各个池中的：
#   - 质押数量
#   - 待领取奖励
#   - 解质押请求详情（数量、解锁区块、状态）
# 示例: USER_ADDRESS=0x1234567890123456789012345678901234567890
# USER_ADDRESS=0x...
